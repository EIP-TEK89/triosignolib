import { MediapipeRunner } from "triosigno-lib";

export class MediapipeRunnerMobile extends MediapipeRunner {
  constructor(
    handTrackModelPath: string,
    bodyTrackModelPath: string,
    faceTrackModelPath: string
  ) {
    
  }
}